MYSQL_HOST=db
MYSQL_PORT=3306
MYSQL_USER=latam
MYSQL_PASSWORD=
MYSQL_DATABASE=latam_job_board

PORT=3000
CORS_ORIGIN=http://localhost:5173

DEEPSEEK_API_KEY=
DEEPSEEK_MODEL=deepseek-v4-flash

# When true, blocks reconfiguring the profile (POST /settings) and adding or
# removing resumes (POST/DELETE /ai/resumes) — read-only demo mode.
LOCK_EDIT=false
